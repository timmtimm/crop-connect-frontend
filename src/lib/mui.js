import { createTheme } from "@mui/material/styles";

export const propsTheme = createTheme({
  palette: {
    primary: {
      main: "#53A06C",
      light: "#8CBF9D",
      dark: "#376A48",
    },
  },
});
